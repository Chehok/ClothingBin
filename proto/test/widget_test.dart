// This is a basic Flutter widget test.
//
// To perform an interaction with a widget in your test, use the WidgetTester
// utility in the flutter_test package. For example, you can send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the widget
// tree, read text, and verify that the values of widget properties are correct.

// import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

// import 'package:proto/main.dart';
import 'package:proto/api/bin_function.dart';
// import 'package:proto/api/member_function.dart';
// import '../../proto/lib/constants/variable.dart';

void main() {
  test('Counter increments smoke test', () async {
    // 주소 가져오는 메서드
    // var address = await getAddress();
    // print(address);
    // print(await getAddress());

    // postClothInfo("imgLink", "clothName", "category", "price", "description", 1);
    // sendSMS("01093858858");

    // naver sms
    // print('getSignature');
    // print(getSignature(Uri.encodeComponent("${serviceId}"), (DateTime.now().millisecondsSinceEpoch).toString(),
    //          "${accessKey}", "${secretKey}"));
    // print("millieseconds");
    // print((DateTime.now().millisecondsSinceEpoch).toString());

    // print(await getAllAddress());
    // print('');
    // print(await getAllClothesInfo(1));
    // print('');
    // print(await getClothInfo(1));

  });
}
